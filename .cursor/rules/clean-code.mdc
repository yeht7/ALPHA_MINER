---
description: 代码精简可读，杜绝冗余
alwaysApply: true
---

# 精简代码规则

## 核心原则

1. **少即是多**：能用一行清晰表达的逻辑不要拆成三行。优先使用语言内置特性和标准库。
2. **不写废话注释**：注释只解释"为什么"，不解释"做了什么"。代码本身应能说明"做了什么"。
3. **不重复自己（DRY）**：每次新增或修改代码前，检查是否已有可复用的逻辑。有则复用，无则抽取。
4. **每次更新时审视冗余**：修改文件时顺便审查周围代码，如果发现可以简化的冗余逻辑，一并清理。

## 具体要求

- 删除未使用的 import、变量、函数
- 避免过度防御式编程（不要给不可能发生的情况写处理逻辑）
- 函数参数不超过 5 个，超过时用 dataclass / TypedDict 封装
- 单个函数不超过 40 行，超过时拆分
- 优先用推导式、内置函数替代手写循环
